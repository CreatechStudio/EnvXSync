name: envxsync
services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: pAssW0rd
    ports:
      - 15432:5432
    volumes:
      - ../backend/test/data/db:/var/lib/postgresql/data
